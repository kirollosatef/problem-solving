#include <iostream>
using namespace std;
int main()
{
  int x, y;
  cin >> x >> y;
  char grid[x][y];
  for (int i = 0; i < x; i++)
    for (int j = 0; j < y; j++)
      cin >> grid[i][j];
  int xs, ys, xg, yg, ans = 0;
  cin >> xs >> ys >> xg >> yg;
  for (int k = 0; k < 1;)
  {
      
  }
}