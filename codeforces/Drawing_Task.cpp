#include <bits/stdc++.h>
#define pb push_back
#define vi vector<int>
#define vs vector<string>
#define v(x) vector<x>
#define sz(v) (int)(v).size()
#define all(str) str.begin(), str.end()
#define clr(x, v) memset(x, v, sizeof(x))
#define rep(i, n) for (int i = 0; i < (int)sz(n); i++)
#define gtar(arr, n) for (int i = 0; i < n; cin >> arr[i], i++)
#define o_a                     \
  ios_base::sync_with_stdio(0); \
  cin.tie(0);
using namespace std;
typedef long long ll;
int main()
{
  o_a;
  int x, y, n;
  cin >> x >> y >> n;
  char ans[x][y];
  clr(ans, '.');
  int sum[20][20];
  cout << "hello";
}